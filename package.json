{
  "name": "solar-system",
  "version": "0.12.0",
  "private": true,
  "type": "module",
  "description": "Симуляция Солнечной системы: реальные расстояния, кеплеровские орбиты, свободный полёт",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "e2e": "playwright test",
    "stars": "node scripts/fetch-stars.mjs",
    "shots": "node scripts/shots.mjs",
    "roadmap:issues": "node scripts/roadmap-to-issues.mjs"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/three": "^0.185.4",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  }
}
